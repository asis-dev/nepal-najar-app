# 2026-03-28

- Implemented civic complaints expansion from partial API to end-to-end flow:
  - Added complaint auth/permission helper in `lib/complaints/access.ts`.
  - Added complaint detail/update API: `GET/PATCH /api/complaints/[id]`.
  - Added complaint timeline API: `GET/POST /api/complaints/[id]/events`.
  - Added complaint evidence API: `GET/POST /api/complaints/[id]/evidence`.
  - Added evidence review API: `PATCH /api/complaints/[id]/evidence/[evidenceId]/review`.
  - Added follow API: `GET/POST/DELETE /api/complaints/[id]/follow`.
  - Added departments lookup API: `GET /api/complaints/departments`.
- Added client hooks in `lib/hooks/use-complaints.ts`.
- Added public UI:
  - `/complaints` list + submit form
  - `/complaints/[id]` detail + follow + update + evidence forms
- Wired nav discoverability:
  - Added complaints entry in top/bottom nav and i18n labels.
  - Added `/complaints` in sitemap and middleware public prefixes.
- Verification:
  - Ran `npm run -w @nepal-progress/admin-web build` successfully.
  - Build includes all new complaint routes/pages.
- Expanded complaints into operations-grade lifecycle:
  - Added migration `032-complaint-operations.sql` for SLA policies, assignments, escalations, department members, expanded notification types.
  - Added complaint helpers:
    - `lib/complaints/sla.ts`
    - `lib/complaints/notifications.ts`
    - updated `lib/complaints/access.ts` and `lib/complaints/types.ts`
  - Added new APIs:
    - `POST /api/complaints/[id]/assign`
    - `POST /api/complaints/[id]/escalate`
    - `POST /api/complaints/[id]/satisfaction`
    - `GET /api/complaints/inbox`
    - `GET /api/complaints/dashboard`
    - `POST /api/complaints/sla-scan`
  - Extended existing complaint APIs to trigger SLA refresh and notifications on status/evidence updates.
  - Added verifier operations UI:
    - `/complaints/ops` page
    - reviewer controls on `/complaints/[id]` (status, assign, escalate)
    - citizen resolution feedback on `/complaints/[id]`
    - nav links for verifiers.
  - Re-ran `npm run -w @nepal-progress/admin-web build` and it passed.
- Re-applied DB migrations directly against Supabase for confirmation:
  - `node scripts/run-pg-migration.js supabase/031-civic-complaints.sql supabase/032-complaint-operations.sql`
  - Both succeeded idempotently (`already exists, skipping` notices only).
- Verified live schema includes complaint operations objects:
  - Tables: `civic_complaints`, `complaint_assignments`, `complaint_department_members`, `complaint_escalations`, `complaint_sla_policies`
  - Columns: `assigned_department_key`, `assigned_user_id`, `sla_due_at`, `sla_breached_at`, `resolved_at`, `closed_at`, `citizen_satisfaction`
- Added citizen voice-input transcription for complaints:
  - New authenticated endpoint: `POST /api/complaints/transcribe` with IP rate limit and media/file-size validation.
  - Reused `transcribeAudioBuffer` in `lib/intelligence/collectors/audio-transcriber.ts` for direct file transcription.
  - Updated `/complaints` submit form voice flow so transcript appends into description and `input_mode` switches correctly (`voice`/`mixed`).
  - Ran `npm run -w @nepal-progress/admin-web build` successfully; route list includes `/api/complaints/transcribe`.
