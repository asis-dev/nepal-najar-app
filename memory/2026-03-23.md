Validated and hardened the Nepal Najar intelligence engine in `apps/admin-web`.

- Fixed the live DB `intelligence_jobs` constraint to allow `process_signals_batch` and updated migrations:
  - `apps/admin-web/supabase/012-intelligence-jobs-and-control.sql`
  - `apps/admin-web/supabase/015-intelligence-job-type-expansion.sql`
  - `apps/admin-web/scripts/run-pg-migration.js`
- Applied the new job-type constraint directly to the database.
- Fixed engine orchestration issues:
  - `processSignalsBatch` now writes `promise_updates.article_id = null` instead of a bad URL/UUID mismatch.
  - sweep now cleans stale sweep rows, queues signal-analysis jobs, skips weak legacy auto-status sync by default, and records real `signals_relevant`.
  - worker now requeues stale locked jobs and processes jobs one at a time instead of claiming the whole queue at once.
  - worker route defaults lowered to safer limits; queued analysis batch size capped at 5 for future sweeps.
  - Tier 1 inter-item sleep reduced from 500ms to 100ms.
- Live validation:
  - fresh sweep cleaned stale sweep rows and collected more signals.
  - analysis-only sweep successfully queued `process_signals_batch` jobs and a `run_status_pipeline` job.
  - worker processing with OpenClaw/local CLI fallback produced the first real `promise_updates` row (`promise_id = 4`, `field_changed = statement`).
  - backlog moved down from the initial post-fix baseline; running sweep rows are now cleaned up.
- Current live state after validation:
  - `promise_updates = 1`
  - `intelligence_status_recommendations = 29`
  - no running sweep rows
  - analysis queue still has pending work plus one running job from validation

Pilot-readiness cleanup:

- Replaced the public `/trending` page placeholder UI with a real data-driven surface using `useTrending`.
- Extended `apps/admin-web/lib/hooks/use-trending.ts` to expose `topSignals` so the page can show real source trails.
- Softened the homepage regional language from `Regional preview beta` to `Regional preview`.
- Removed `priority` loading from the homepage Balen image and added responsive sizes for lighter initial load.
- Build still passes after the pilot-facing updates.
- Live queue cleanup:
  - resized pending `process_signals_batch` jobs from batch size `10` to `5`
  - requeued the stale validation-locked analysis job
  - current queue state: `7` pending analysis jobs, `1` pending status pipeline job, `0` running sweeps
  - `promise_updates` increased to `2`

Pilot tracker setup:

- Added `apps/admin-web/supabase/016-pilot-analytics.sql` and applied it live to create `pilot_events`.
- Added public analytics capture:
  - `apps/admin-web/lib/analytics/events.ts`
  - `apps/admin-web/lib/analytics/client.ts`
  - `apps/admin-web/components/public/pilot-analytics.tsx`
  - `apps/admin-web/app/api/analytics/events/route.ts`
- Mounted page-view tracking in public and landing layouts.
- Instrumented key pilot actions:
  - hometown set / watchlist add-remove in `apps/admin-web/lib/stores/preferences.ts`
  - feedback submit in `apps/admin-web/app/(public)/feedback/page.tsx`
  - citizen proof submit in `apps/admin-web/components/public/share-proof-button.tsx`
  - progress verification submit in `apps/admin-web/components/public/verify-progress.tsx`
- Added internal dashboard tracker:
  - `apps/admin-web/lib/data/pilot-tracker.ts`
  - `apps/admin-web/app/(dashboard)/pilot/page.tsx`
  - sidebar nav entry for `/pilot`
- Build passes after tracker changes.
- First live tracker baseline right after launch:
  - `pilot_events = 0`
  - `unique visitors = 0`
  - `pending jobs = 7`
  - `pending status recommendations = 29`
  - `promise_updates in 14d = 2`
  - `public commitments = 109`

OpenClaw pilot tracker autopilot:

- Added `apps/admin-web/supabase/017-pilot-summary-autopilot.sql` and applied it live.
- Added `apps/admin-web/lib/intelligence/pilot-summary.ts` so OpenClaw can read pilot metrics and persist founder-facing summaries.
- Added a new `summarize_pilot_tracker` intelligence job type and wired it through:
  - `apps/admin-web/lib/intelligence/jobs.ts`
  - `apps/admin-web/app/api/intelligence/worker/route.ts`
  - `apps/admin-web/app/api/admin/pilot-summary/route.ts`
- Added the dashboard panel `apps/admin-web/components/dashboard/pilot-summary-panel.tsx` to `/pilot`.
- Validated a live OpenClaw-generated pilot summary row:
  - provider `openclaw`
  - model `gpt-5.3-codex`
  - latest summary headline: `Tracking is live, but pilot traffic has not started yet.`
  - wins called out: `109 reviewed public commitments`, `2 commitment updates`
  - watch items called out: `0 pilot traffic yet`, `7 pending jobs`, `29 pending status recommendations`
- Local OpenClaw CLI can time out on long embedded runs, so the pilot summary path now falls back to a deterministic useful summary when the CLI returns low-signal timeout output.
- `npm exec tsc -- -p apps/admin-web/tsconfig.json --noEmit` passes after the pilot summary work.
