Implemented feedback review autopilot for `apps/admin-web`.

- Added `user_feedback` AI review fields via `apps/admin-web/supabase/014-feedback-autopilot.sql`.
- Added OpenClaw-powered feedback triage backend in `apps/admin-web/lib/intelligence/feedback-review.ts`.
- Added admin feedback review APIs and dashboard inbox at `/feedback-review`.
- Added `review_feedback` intelligence job support in the worker/job system.
- Applied migration `014-feedback-autopilot.sql` to Supabase successfully.
- Added dedicated OpenClaw CLI agent support via `OPENCLAW_AGENT` in `ai-router.ts`.
- Created dedicated local OpenClaw agent `nepal-najar-autopilot` and set `OPENCLAW_AGENT=nepal-najar-autopilot` in `apps/admin-web/.env.local`.
- Validated end-to-end feedback autopilot with a temporary feedback row:
  - OpenClaw provider: `openclaw`
  - model: `gpt-5.3-codex`
  - recommendation: `feature_request`
  - priority: `high`
  - flow validated: `reviewed -> approved -> applied`
- Cleaned up all temporary validation feedback rows afterward.
- Fixed unrelated build blockers in `reddit-scraper.ts` and `sweep.ts`; `npm run -w @nepal-progress/admin-web build` passes.
