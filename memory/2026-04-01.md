# 2026-04-01

- Reviewed latest Nepal Republic code updates after major user changes.
- Verified current state:
  - `git status` shows 63 changed items (13 untracked additions).
  - `next build` passes in `apps/admin-web` with 2,373 generated static pages and all new routes present.
  - Lint command is currently not runnable non-interactively because `next lint` prompts for first-time ESLint setup.
- Key strengths observed:
  - Expanded public surface (`/me`, `/what-changed`, embeds, ministers, corruption, llms routes).
  - Better daily brief audio alignment (script now derived from visible brief text).
  - SEO footprint significantly expanded (structured data + large sitemap).
- Key issues flagged for follow-up:
  - Side-effect in render path in onboarding checklist (`setTimeout` inside render branch).
  - `.well-known/ai-plugin.json` advertises `openapi` but points to `/llms.txt` (not an OpenAPI spec).
  - `/api/social-post` ignores `lang` in content generation and uses static promises/stats (risk of stale public posts).
  - `/api/og/province` labels output as province report while using national aggregate stats.
  - Hardcoded public claims in SEO/LLMS text (`109 commitments`, `80+ sources`) can drift from dynamic data.
  - Duplicate background component rendered in both global providers and public layout.
- Implemented all flagged fixes:
  - Moved onboarding auto-dismiss timer into `useEffect` to remove render-time side effects.
  - Added real OpenAPI endpoint at `/openapi.json` and pointed `.well-known/ai-plugin.json` to it.
  - Updated `/api/social-post` to use live commitments (`getPromises` fallback to static), respect `lang=en|ne`, and pick localized daily-brief summaries.
  - Updated province OG card generation to use province-tagged commitments (not national aggregate fallback) with explicit no-data messaging.
  - Removed brittle hardcoded count claims in `seo.ts`, `llms.txt`, and `llms-full.txt`; `llms.txt` now resolves commitment count dynamically when available.
  - Removed duplicate `CivicSkyBackground` mount from global providers (public layout remains the background owner).
- Verification:
  - Re-ran `npm run -w @nepal-progress/admin-web build` successfully after fixes.
- Updated onboarding return-user behavior:
  - `OnboardingGate` no longer auto-opens the welcome walkthrough for signed-in users; it now marks onboarding complete silently and returns `null` so users land directly on dashboard.
  - `WelcomeFlow` greeting now resolves name from multiple sources (`profile.displayName`, auth metadata name/display_name, email handle) before falling back to `friend`/`साथी`.
  - Build passes after this change (`next build` successful; same pre-existing lockfile patch warning remains).
- Upgraded sharing and social preview foundation:
  - Added unified share URL normalization + UTM tracking + platform intent helpers in `lib/utils/share.ts`.
  - Added reusable `ShareComposerModal` with optional user comment and one-click X/Facebook/WhatsApp/native/copy actions.
  - Updated card-level share actions to open composer modal instead of fire-and-forget share call, fixing external URL sharing edge case from article cards.
  - Upgraded project detail share panel to support optional comment + tracked share links.
  - Added `share_clicked` analytics event type.
  - Standardized metadata for key public sections (`about`, `how-it-works`, `leaderboard`, `notifications`, `proposals`, `reputation`, `support`, `trending`, `watchlist`) using `createMetadata(...)` with canonical path to keep OG/Twitter cards consistent.
  - Build passes after changes.
- Share consistency catch-up pass:
  - Replaced remaining manual `navigator.share` / hardcoded social URL builders in key public pages with unified share helpers (`shareOrCopy` / `shareIntentUrl`) for consistent tracking/link behavior.
  - Standardized icon glyph to `Share` (matching cabinet minister card style) by removing remaining `Share2` usage.
  - Files touched for this pass include daily, proposals detail, report-card, first-100-days list/detail, feed-island, and share-composer modal.
  - Build passes after this pass as well.
- Mobile tab clarity update:
  - Updated bottom mobile nav from icon-only to icon + short label per tab (localized en/ne), with clearer icon mapping (Home, Tracker, Report, Cabinet, Issues, Me).
  - Added aria-label/title for accessibility and raised nav bar height slightly to fit labels cleanly.
  - Build passes after this update.
- Additional hardening pass completed:
  - Added centralized middleware security headers on all non-static responses (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, and production `Strict-Transport-Security`).
  - Kept Supabase session-first admin gating and explicit legacy admin-secret opt-in path.
  - Updated admin build script to `NEXT_IGNORE_INCORRECT_LOCKFILE=1 next build` to remove noisy non-fatal lockfile auto-patch warnings in CI/local builds.
  - Re-verified `next build` succeeds with 2,374 static pages and no lockfile patch warning.
- Security hardening follow-up (no UX/login friction):
  - Added shared secure auth helper (`lib/security/request-auth.ts`) with constant-time secret comparison and bearer token extraction.
  - Switched privileged intelligence/scrape/notification routes from plain string secret comparison to constant-time checks.
  - Added same-origin browser-context enforcement for session-based scrape/admin triggers in `validateScrapeAuth` (with env escape hatch: `ENFORCE_SCRAPE_ORIGIN_CHECK=false`).
  - Added `Cache-Control: no-store` on standardized scrape unauthorized responses.
  - Updated `.env.example` to document `ENFORCE_SCRAPE_ORIGIN_CHECK`.
  - Verified with `next build`, `eslint`, and `jest --passWithNoTests` (build/test pass, lint warnings unchanged/no errors).
